[run]
# Coverage configuration for the tailor-resume skill.
#
# All previously-omitted modules (parsers/*, stores/*, claude_vision_extractor)
# now have integration coverage. As of issue #79 the heavier Claude-vision /
# multi-backend fallback paths in parsers/pdf_extractor.py are also covered,
# so no source files are excluded from the report.
